import { styled } from '@mui/material/styles';
import Button from '@mui/material/Button';

const SaprayIconButton = styled(Button)(() => ({
  borderRadius: "50%",
  height: 40,
  width: 40,
  minHeight: 40,
  minWidth: 40,
  maxHeight: 40,
  maxWidth: 40,
}));

export default SaprayIconButton;